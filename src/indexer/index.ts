import escrows from "./escrows";
import releaseEscrows from "./release-escrows";
import sync from "./sync";

export const releaseEscrowIndex = releaseEscrows;
export const escrowIndex = escrows;
export const syncIndex = sync;
